import { expect } from 'chai';
import 'mocha';
import { CovidIndiaApiClient } from './index'
describe('Test CovidIndiaApiClient ', () => {

    it('should call the dialog flow api', async () => {
        expect(true).to.deep.equal(true)
    })
})